// This file is no longer used - see app/not-found.tsx instead
// Keeping this as a placeholder to prevent import errors in case it's referenced elsewhere
const NotFound = () => {
  return null;
};

export default NotFound;
